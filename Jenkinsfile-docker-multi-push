pipeline {
  agent any

  stages {
    
    stage('Build Image') {
      steps {
      sh "docker build -t pathan1216/request-logger:${env.BUILD_ID} -f Dockerfile-multi ."
      sh "docker tag pathan1216/request-logger:${env.BUILD_ID} brainupgrade/request-logger:latest"
      }
    }
    
  }

  post {
    success {
      withCredentials([usernamePassword(credentialsId: 'docker-hub-credentials', usernameVariable: 'USERNAME', passwordVariable: 'PASSWORD')]) {
        sh "docker login -u ${USERNAME} -p ${PASSWORD}"
        sh "docker push pathan1216/request-logger:${env.BUILD_ID}"
        sh "docker push pathan1216/request-logger:latest"
      }
    }
  }
}
